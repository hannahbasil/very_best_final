<!-- Responsive panel example-->

<div class="row">
  <div class="col-sm-offset-1 col-sm-10 col-md-offset-2 col-md-8">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">Dish #<%= @dish.id %></h3>
      </div>
      <div class="panel-body">
        <div class="row">
          <div class="col-md-12">
            <dl class="dl-horizontal">
              <dt>Cuisine</dt>
              <dd><%= @dish.cuisine.name %></dd>

              <dt>Name</dt>
              <dd><%= @dish.name %></dd>

            </dl>
            <div class="btn-group btn-group-justified">
              <%= link_to icon('chevron-left'), dishes_path, class: "btn btn-primary" %>

              <%= link_to icon('edit'), edit_dish_path(@dish), class: "btn btn-warning" %>

              <%= link_to icon('trash-o'), @dish, method: :delete, data: { confirm: "Do you really want to delete this dish?" }, class: "btn btn-danger" %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- End responsive panel example -->
